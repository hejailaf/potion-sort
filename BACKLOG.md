# Backlog — ideas deliberately not built

Per docs/V1.3_BRIEF.md: tempting out-of-scope ideas land here instead of in the build.

- **Modifier stacking** (multiple mechanics on one level): the `LevelDef.modifiers` array
  already supports it structurally; v1.3 caps at one per level (`modifiersFor`). Needs its
  own difficulty tuning + solver load check before enabling.
- Full pour chaining (pick up a mid-pour bottle) — pre-v1.3 idea, still open.
- Real Teams tab (currently placeholder).
- Licensed SFX pass if the synthesized pour underwhelms.
- Expo SDK upgrade (evaluate after v1.3).
