// design-sync web entry: the ui/ primitives, re-exported for the react-native-web
// bundle that Claude Design imports. Skia/reanimated game components are excluded —
// they don't render in a browser runtime.
export { GameButton } from '@/components/ui/GameButton';
export { IconButton } from '@/components/ui/IconButton';
export { GameModal } from '@/components/ui/GameModal';
